<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blogs</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/allblogs.css">
</head>
<body>
    <div class="recent-posts">
        <h2>ALL POSTS</h2>
        <%for (let i=0 ; i< blogs.length ; i++){%>
            <div class="post-card">
                <a href="/blog/<%= blogs[i]._id %>" class="post-title"><%= blogs[i].title %></a>
                <p class="post-date">
                    <%= blogs[i].createdAt.toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %>
                </p>
            </div>
        <%}%>
    </div>
</body>
</html>